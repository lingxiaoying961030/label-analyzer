# Label Analyzer UI 改进方案

## 改进目标

基于8个真实config分析，解决两个问题：
1. **Config预览不好看** — 字段多/嵌套深时直接罗列太丑
2. **分析配置区域需优化** — 字段多时选择不直观

---

## 一、Config 预览区改进

### 现状问题
- 所有字段平铺在一个列表里，没有层级感
- 选项信息挤在一行末尾，字段多了看不清
- depend类型有子字段，但完全没体现嵌套关系

### 改进方案：左右分栏布局

```
┌─────────────────────────────────────────────────────┐
│  CONFIG 概览                                        │
│  项目名: 指令遵循评测集v3-数量统计annotate              │
│  字段数: 3 | 类型: depend, form_list, textarea        │
├──────────────────┬──────────────────────────────────┤
│  字段列表(左)     │  字段详情(右)                      │
│                  │                                  │
│  ┌────────────┐  │  ┌────────────────────────────┐  │
│  │🔘 遵循判断  │◄─┤  │ 遵循判断                    │  │
│  │  depend     │  │  │ 类型: depend (组合控件)      │  │
│  ├────────────┤  │  │                            │  │
│  │📋 数量统计  │  │  │ 子字段:                     │  │
│  │  form_list  │  │  │ ├─ 检查项合理性判断 (radio)   │  │
│  ├────────────┤  │  │ ├─ 回复1遵循判断 (radio)      │  │
│  │📝 笔记     │  │  │ └─ 回复2遵循判断 (radio)      │  │
│  │  textarea   │  │  │                            │  │
│  └────────────┘  │  │ visibleRule: 0-0-0          │  │
│                  │  │ (所有model都显示)             │  │
│                  │  └────────────────────────────┘  │
└──────────────────┴──────────────────────────────────┘
```

#### 设计要点

**左侧 - 字段列表：**
- 每个字段一个紧凑卡片，显示：图标 + 标题 + 类型标签
- 点击高亮，右侧显示详情
- 类型用不同颜色的小标签区分：
  - 🔘 radio/checkbox → 紫色
  - 📝 textarea系列 → 绿色  
  - 🔗 depend → 蓝色
  - 📋 form_list → 橙色
  - 🏷️ text_label → 青色
  - 📎 其他(link/screenshot/ranking) → 灰色

**右侧 - 字段详情：**
- 显示字段完整信息
- 有options的：展示选项列表（带value映射）
- 有子字段的：用缩进树形展示嵌套结构
- 有条件依赖的：显示依赖关系（如"当is_skip=否时显示"）
- 有visibleRule的：显示规则含义

#### 不同字段数量的适配

| 字段数 | 表现 |
|--------|------|
| 1-3个 | 默认选中第一个，右侧自动展示 |
| 4-6个 | 左侧列表紧凑排列，不需要滚动 |
| 7+个 | 左侧列表可滚动，固定高度 |

---

## 二、分析配置区改进

### 现状问题
- Model选择和字段选择平铺在一起
- 字段多了会拉长，没有分类
- 没有字段类型提示，不知道哪些字段能分析什么

### 改进方案：分组卡片式选择

```
┌─────────────────────────────────────────────────────┐
│  分析配置                                            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Model 选择                                         │
│  ┌──────┐ ┌──────────┐ ┌──────────┐                │
│  │☑ 全选│ │☑ judgment│ │☑ response│                │
│  └──────┘ └──────────┘ └──────────┘                │
│                                                     │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│                                                     │
│  分析字段选择                              全选 / 清空│
│                                                     │
│  📊 可统计分布的字段                                  │
│  ┌──────────────────┐ ┌──────────────────┐         │
│  │☑ judgment质量判定 │ │☐ 回复1遵循判定    │         │
│  │   多选 · 3个选项  │ │   单选 · 2个选项  │         │
│  └──────────────────┘ └──────────────────┘         │
│                                                     │
│  📝 文本类字段                                       │
│  ┌──────────────────┐ ┌──────────────────┐         │
│  │☐ 笔记            │ │☐ rubric分析(回复1)│         │
│  │   文本框          │ │   文本框          │         │
│  └──────────────────┘ └──────────────────┘         │
│                                                     │
│  🏷️ 标注类字段                                      │
│  ┌──────────────────┐                              │
│  │☐ 内部逻辑判断     │                              │
│  │   文本标注 · 2子标签│                              │
│  └──────────────────┘                              │
│                                                     │
│  📋 列表/其他字段                                    │
│  ┌──────────────────┐ ┌──────────────────┐         │
│  │☐ 数量统计         │ │☐ 排序            │         │
│  │   表单列表 · 5列  │ │   排序控件         │         │
│  └──────────────────┘ └──────────────────┘         │
│                                                     │
│                              [ 🚀 开始分析 ]         │
└─────────────────────────────────────────────────────┘
```

#### 字段分组规则

按**分析方式**自动分成4组：

| 分组 | 图标 | 包含的valueType | 说明 |
|------|------|----------------|------|
| 可统计分布 | 📊 | radio, checkbox, depend(含radio子字段) | 有选项，能画饼图/柱状图 |
| 文本类 | 📝 | textarea, textarea_auto_height, text, draggable_md_editor | 分析非空率、字数 |
| 标注类 | 🏷️ | text_label | 分析子标签分布 |
| 列表/其他 | 📋 | form_list, responses_ranking, screenshot, editable_link | 特殊类型 |

#### 设计要点

- 每个字段卡片显示：名称 + 类型 + 选项数/子字段数
- 分组标题带图标，一目了然
- 支持**全选/清空**快捷操作
- depend类型的字段，如果子字段是radio，归入"可统计分布"组（扁平化展示子字段）

---

## 三、整体布局调整

### 视觉层级

```
1. 标题栏 ─────────── 固定顶部
2. 上传区域 ────────── 两列卡片（config + data）
3. Config预览 ──────── 左右分栏（字段列表 + 详情）
4. 分析配置 ────────── 分组卡片选择
5. 分析结果 ────────── 结果卡片（图表+表格）
```

### 配色方案（沿用暗色主题）

| 类型 | 颜色 | 用途 |
|------|------|------|
| radio/checkbox | `#6c5ce7` 紫色 | 选择类控件标签 |
| textarea系列 | `#00b894` 绿色 | 文本类控件标签 |
| depend | `#74b9ff` 蓝色 | 组合控件标签 |
| form_list | `#fdcb6e` 橙色 | 列表控件标签 |
| text_label | `#00cec9` 青色 | 标注控件标签 |
| 其他 | `#636e72` 灰色 | 特殊控件标签 |

---

## 四、交互细节

1. **Config预览**
   - 默认选中第一个字段
   - 点击字段切换右侧详情（带滑入动画）
   - 无需滚动时隐藏滚动条

2. **分析配置**
   - 分组可折叠/展开
   - 勾选字段时有轻微动画反馈
   - 已选字段数实时显示："已选 3/6 个字段"
   - 如果某字段在所选Model中无数据，灰显提示

3. **响应式**
   - 窄屏时Config预览从左右分栏变为上下布局
   - 字段选择从多列变单列
